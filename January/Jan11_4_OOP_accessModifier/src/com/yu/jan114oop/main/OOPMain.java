package com.yu.jan114oop.main;
// access modifier
//		접근제어자/지정자/제한자
// (메소드포함)멤버들 접근을 제어할 때 쓰는 것들

// 4가지 종류가 있다(철저히 Java의 경우임, 다른 언어는 다를 수 있음)

// 이론적으로는 그런데, protected가 안 쓴거랑 사실상 같아져버리는, Java의 유명한 버그가 있음
// public : 아무제한 없이 어디서든 쓸 수 있음
// protected : 같은 패키지 or 다른 패키지지만 상속관계면 쓸 수 있음
// 안쓰기(friendly, default) : 같은 패키지내에서만 접근 가능
// private : 외부에서 못 쓰게(class 내부에서는 쓸 수 있음)

public class OOPMain {
	public static void main(String[] args) {
		
	}
}
